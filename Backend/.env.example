# WorCoor Backend Environment Variables
# Copy this file to .env and fill in the values

# ===========================================
# Application Settings
# ===========================================
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
CORS_ORIGIN=http://localhost:5173

# ===========================================
# Supabase Configuration
# Get these from: Supabase Dashboard → Settings → API
# ===========================================
SUPABASE_PROJECT_ID=your_project_id
SUPABASE_URL=https://your_project_id.supabase.co
SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ===========================================
# Database Connection
# Get from: Supabase Dashboard → Settings → Database → Connection string (URI)
# Replace [YOUR-PASSWORD] with your actual database password
# ===========================================
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your_project_id.supabase.co:5432/postgres

# ===========================================
# Authentication Secrets
# Generate secure random strings (32+ characters)
# Use: openssl rand -hex 32
# ===========================================
JWT_SECRET=generate_a_secure_32_character_random_string
COOKIE_SECRET=generate_a_secure_32_character_random_string

# ===========================================
# Optional: Redis (for Socket.IO horizontal scaling)
# Only needed in production with multiple server instances
# ===========================================
# REDIS_URL=redis://default:password@your_upstash_redis_url:6379
